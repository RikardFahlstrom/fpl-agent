"""The MCP server: tools, resources, prompts, and the local login page.

One interface onto the engine rather than the product itself. Importing `tools`,
`resources` and `prompts` is what registers them with the server.
"""
